{
  "name": "feedback-app",
  "version": "1.0.0",
  "description": "FEEDBACK — Social network for parties, rave culture & nightlife",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build:icons": "node scripts/build-icons.mjs",
    "prebuild": "node scripts/build-icons.mjs",
    "build": "vite build",
    "preview": "vite preview --host --port ${PORT:-3000}",
    "start": "vite preview --host --port ${PORT:-3000}",
    "supabase:start": "supabase start",
    "supabase:stop": "supabase stop",
    "supabase:reset": "supabase db reset",
    "supabase:push": "supabase db push",
    "supabase:link": "supabase link --project-ref $SUPABASE_PROJECT_REF",
    "supabase:deploy": "supabase db push && supabase functions deploy bold-webhook --no-verify-jwt && supabase functions deploy create-bold-checkout && supabase functions deploy send-push && supabase functions deploy notify-support-ticket"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "vite": "^5.4.21"
  }
}
